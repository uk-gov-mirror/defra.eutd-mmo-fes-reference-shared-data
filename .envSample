BOOMI_URL =
REF_BOOMI_CATCH_API_TIMEOUT_MS =
SKIP_AV_SCAN =

# --- Boomi API authentication ---
# Local development only: set these three vars to use OAuth2 client credentials.
# Leave them unset in Azure — Managed Identity is used automatically instead.
REF_BOOMI_API_OAUTH_CLIENT_ID=
REF_BOOMI_API_OAUTH_CLIENT_SECRET=
REF_BOOMI_API_OAUTH_TOKEN_URL=

# OAuth scopes (required in all environments)
REF_BOOMI_API_OAUTH_AL_SCOPE=
REF_BOOMI_API_OAUTH_MMO_SCOPE=
REF_BOOMI_CATCH_API_OAUTH_SCOPE=

REF_BOOMI_USER=

REFRESH_SPECIES_JOB=
REFRESH_VESSEL_JOB=
INSTRUMENTATION_CLOUD_ROLE=
DB_NAME=
DB_CONNECTION_POOL=
EXTERNAL_APP_URL=
INTERNAL_ADMIN_URL=
AZURE_BLOB_CONTAINER=
REPORT_QUEUE=
REPORT_QUEUE_TRADE=
ALL_SPECIES_DATA_CONTAINER_NAME=
ALL_SPECIES_DATA_FILE_NAME=

ENABLE_CHIP_REPORTING=
